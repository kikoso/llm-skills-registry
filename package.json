{
  "name": "findskills-dev",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run generate-registry && next dev",
    "build": "npm run generate-registry && next build",
    "generate-registry": "node scripts/generate-registry.mjs",
    "lint": "node scripts/lint-skills.mjs && next lint",
    "start": "next start"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "glob": "latest"
  }
}
